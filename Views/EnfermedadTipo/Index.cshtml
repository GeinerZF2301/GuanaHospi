@model IEnumerable<GuanaHospi.Models.EnfermedadTipo>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdEnfermedadNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoEnfermedadNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdEnfermedadNavigation.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoEnfermedadNavigation.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEnfermedadTipo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEnfermedadTipo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEnfermedadTipo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
