@model IEnumerable<GuanaHospi.Models.IntervencionTipo>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdIntervencionNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoIntervencionNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdIntervencionNavigation.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoIntervencionNavigation.NombreTipo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdIntervencionTipo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdIntervencionTipo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdIntervencionTipo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
