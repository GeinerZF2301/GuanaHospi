@model IEnumerable<GuanaHospi.Models.PacienteSintoma>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdPacienteNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdSintomaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdPacienteNavigation.Apellido1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdSintomaNavigation.Descripcion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPacienteSintoma">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPacienteSintoma">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPacienteSintoma">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
